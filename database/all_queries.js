var clusterQueries = require('./query/cluster');
var companyQueries = require('./query/company');
var composed_ofQueries = require('./query/composed_of');
var deskQueries = require('./query/desk');
var emailQueries = require('./query/email');
var employeeQueries = require('./query/employee');
var floor_planQueries = require('./query/floor_plan');
var has_a_cluster_tempQueries = require('./query/has_a_cluster_temp');
var has_a_emp_tempQueries = require('./query/has_a_emp_temp');
var initializationQueries = require('./query/initialization');
var loginQueries = require('./query/login');
var managesQueries = require('./query/manages');
var officeQueries = require('./query/office');
var organized_byQueries = require('./query/organized_by');
var owned_byQueries = require('./query/owned_by');
var password_resetQueries = require('./query/password_reset');
var rangeQueries = require('./query/range');
var sits_atQueries = require('./query/sits_at');
var usesQueries = require('./query/uses');
var works_atQueries = require('./query/works_at');
var collection = {};

for(var item in clusterQueries) collection[item] = clusterQueries[item];
for(var item in companyQueries) collection[item] = companyQueries[item];
for(var item in composed_ofQueries) collection[item] = composed_ofQueries[item];
for(var item in deskQueries) collection[item] = deskQueries[item];
for(var item in emailQueries) collection[item] = emailQueries[item];
for(var item in employeeQueries) collection[item] = employeeQueries[item];
for(var item in floor_planQueries) collection[item] = floor_planQueries[item];
for(var item in has_a_cluster_tempQueries) collection[item] = has_a_cluster_tempQueries[item];
for(var item in has_a_emp_tempQueries) collection[item] = has_a_emp_tempQueries[item];
for(var item in initializationQueries) collection[item] = initializationQueries[item];
for(var item in loginQueries) collection[item] = loginQueries[item];
for(var item in managesQueries) collection[item] = managesQueries[item];
for(var item in officeQueries) collection[item] = officeQueries[item];
for(var item in organized_byQueries) collection[item] = organized_byQueries[item];
for(var item in owned_byQueries) collection[item] = owned_byQueries[item];
for(var item in password_resetQueries) collection[item] = password_resetQueries[item];
for(var item in rangeQueries) collection[item] = rangeQueries[item];
for(var item in sits_atQueries) collection[item] = sits_atQueries[item];
for(var item in usesQueries) collection[item] = usesQueries[item];
for(var item in works_atQueries) collection[item] = works_atQueries[item];

module.exports = collection;
